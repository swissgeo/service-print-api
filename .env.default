# used for local development
AWS_REGION=eu-central-1
AWS_DEFAULT_REGION=eu-central-1
AWS_LOCAL=local # "local", "aws_poc", or "whatever" for k8s
LOCALSTACK_PORT=4566 # port for local AWS services such as dynamodb and sqs
HTTP_PORT=3000 # port for make serve or make gunicorn serve
